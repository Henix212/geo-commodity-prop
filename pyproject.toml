[project]
name = "geo-commodity-prop"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "torch-geometric",
    "networkx",
    "pandas",
    "numpy",
    "transformers",
    "requests",
    "beautifulsoup4",
    "yfinance",
    "vectorbt",
    "pyyaml",
    "huggingface-hub>=1.31.0",
    "matplotlib>=3.10.9",
    "accelerate>=1.15.0",
    "plotly<6",
    "streamlit>=1.64.0",
    "pyvis>=0.3.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["backend"]

[project.scripts]
geo-commodity = "backend.main:main"
